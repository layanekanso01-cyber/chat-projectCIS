[pytest]
markers =
    integration: needs a live Weaviate connection (embedding model, reranker, retrieval)
    e2e: needs live Weaviate + Ollama, slow (makes real generation calls)
testpaths = tests
